@model IEnumerable<LojaSite.Models.Marca>

<p>
    @Html.ActionLink("Create New", "Cadastrar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nm_marca)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nm_marca)
        </td>
        <td>
            @Html.ActionLink("Editar", "Editar", new { id=item.Id_marca }) |
            @Html.ActionLink("Consultar", "Consultar", new { id=item.Id_marca }) |
            @Html.ActionLink("Excluir", "Excluir", new { id=item.Id_marca })
        </td>
    </tr>
}

</table>
