@model IEnumerable<LojaSite.Models.Mercado>

<p>
    @Html.ActionLink("Cadastrar Novo Mercado", "Cadastrar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_mercado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nm_mercado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_mercado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nm_mercado)
        </td>
        
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_mercado }) |
            @Html.ActionLink("Consultar", "Consultar", new { id=item.Id_mercado }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_mercado })
        </td>
    </tr>
}

</table>
